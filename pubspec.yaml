name: math_alarm_app_new
description: Una aplicación de alarma que requiere resolver problemas matemáticos para apagarla.

publish_to: 'none' # Evita publicar en pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  flutter_local_notifications: ^19.1.0
  shared_preferences: ^2.2.2
  provider: ^6.1.1
  intl: ^0.20.2
  timezone: ^0.10.1
  audioplayers: ^6.4.0
  permission_handler: ^12.0.0+1
  # flutter_ringtone_player: ^última_versión  # Eliminado
  vibration: ^3.1.3
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  file_picker: ^5.2.10


dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  hive_generator: ^2.0.0
  build_runner: ^2.3.3
  mockito: ^5.3.2

flutter:
  uses-material-design: true
  assets:
    - assets/sounds/
    - assets/images/
